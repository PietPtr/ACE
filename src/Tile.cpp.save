#include "Tile.h"

using namespace sf;

Tile::Tile(int _textureIndex, Vector2f _offset, Vector2f _tileSize, Vector2f _unwalkableOffset, Vector2f _unwalkableSize);
{
    textureIndex = _textureIndex;
    offset = _offset;
    tileSize = _tileSize;
    unwalkableOffset = _unwalkableOffsetl
}
